# 수동 테스트 매트릭스

릴리스 전에 실제 Windows 에서 확인하는 목록입니다. 자동 테스트(`dotnet test`)는 순수 로직만 검증하고,
caret 탐색·IME 판정·창 z-order 는 실제 앱에서만 확인할 수 있습니다.

표기: ✅ 정상 · ⚠️ 알려진 한계 · ❌ 버그 · ⬜ 미확인

## 1. 앱별 배지 표시

| 앱 | 입력 위치 | caret 경로 | 한↔영 전환 | 위치 | 비고 |
|---|---|---|---|---|---|
| 메모장 (Win11) | 본문 | Win32 | ⬜ | ⬜ | |
| Word | 본문 | Win32 | ⬜ | ⬜ | |
| Excel | 셀 편집 | Win32 | ⬜ | ⬜ | 셀 선택 상태에서는 caret 없음 |
| Chrome | 주소창 | UIA text | ⬜ | ⬜ | "문서 처음~커서" 범위 접기 |
| Chrome | 웹 페이지 textarea | UIA text | ⬜ | ⬜ | |
| Edge | 주소창 / 본문 | UIA text | ⬜ | ⬜ | |
| VS Code | 편집기 | UIA text | ⬜ | ⬜ | Electron |
| 카카오톡 PC | 대화 입력창 | ⬜ | ⬜ | ⬜ | |
| Windows Terminal | 셸 | UIA | ⬜ | ⬜ | TSF 전역 compartment 우선. `--debug` 로그에 `tsf(open=… conv=…)` 확인 |
| 설정(UWP) | 검색창 | UIA | ⬜ | ⬜ | CoreWindow 스레드 조사 + TSF 우선. 로그에 `uwp` 표시 |
| 메모장(Win11, IMM 창 없는 상태) | 본문 | Win32 | ⬜ | ⬜ | IMM 이 `imeWnd=0` 이면 TSF 로 넘어가는지(로그) |
| Xshell | 터미널 | IMM 조합 창 → 모서리 | ⬜ | ⬜ | 로그에 `caret:imm(...)` 이면 커서를 따라가는지(영문 타이핑·Enter 뒤에도), `corner` 이면 창 왼쪽 아래 고정. "커서를 못 찾는 앱" 목록에서 Xshell* 을 빼면 숨겨지는지 |
| FlowLauncher / PowerToys Run | 입력창 | ⬜ | ⬜ | ⬜ | TopMost 창 위로 올라가는지 |
| 원격 데스크톱(mstsc) 안 | 원격 앱 | ⬜ | ⬜ | ⬜ | 제외 목록에 mstsc 넣으면 숨김 |
| 전체 화면 게임 | - | ⬜ | - | - | "전체 화면 숨김" 켜면 안 보여야 함 |
| 비밀번호 입력창 | - | ⬜ | ⬜ | ⬜ | 배지는 떠도 됨(글자 노출 아님) |

## 2. 설정·트레이

| 항목 | 확인 내용 | 결과 |
|---|---|---|
| 모양 5종 | 각각 즉시 반영, 재시작 후 유지 | ⬜ |
| 위치 위/아래/왼쪽 | 위쪽 공간이 없으면 아래로, 왼쪽 공간이 없으면 오른쪽으로 | ⬜ |
| 그림자 | 파란 선택 영역 위의 파란 배지도 윤곽이 보임 | ⬜ |
| 애니메이션 | 배지가 처음 뜰 때 페이드인, 한/영 전환 시 살짝 커졌다 돌아옴. 설정 또는 Windows "애니메이션 효과"를 끄면 즉시 표시 | ⬜ |
| 크기 80~160%, 설정 창 50~300% | | ⬜ |
| 불투명도 | 50% 에서 배지 배경 뒤 글자가 비치고 배지 글자는 또렷함 | ⬜ |
| 색상 변경 | 미리보기와 실제 배지가 같음. 노란색을 고르면 글자가 검은색으로 바뀜 | ⬜ |
| 미리보기 | 위치 4종이 caret 기준 위/아래·왼쪽/오른쪽에 그대로 놓임(아래로 밀리지 않음). 불투명도 50% 에서 옆줄 글자가 비침. 크기 200% 까지 잘리지 않음. DotFlash 는 글자 배지 → 1.5초 뒤 점. 슬라이더를 끌어도 깜빡이지 않음 | ⬜ |
| 트레이 프리셋 | 설정 창에서 90% 로 두면 크기 메뉴에 "사용자 지정 (90%)" 가 체크되어 보임 | ⬜ |
| 일시 중지 | 메뉴·Ctrl+Alt+H·트레이 아이콘 회색 + 일시 정지 표시, 툴팁 "일시 중지" | ⬜ |
| 트레이 상태 아이콘 | 메모장에서 한/영 전환 시 트레이 아이콘이 한/A 로 바뀜. 바탕화면 클릭 시 커서 아이콘으로 복귀. 설정에서 끄면 항상 커서 아이콘 | ⬜ |
| Caps Lock | 영문 모드에서 Caps Lock 을 켜면 배지가 `ABC`, 트레이가 `A` + 밑줄, 툴팁·메뉴 상태 줄에 "(Caps Lock)". 한글 모드에서는 변화 없음. 설정에서 끄면 항상 `A` | ⬜ |
| 트레이 클릭 | 왼쪽 클릭 → 설정 창. 메뉴 맨 위에 "현재: …" 상태 줄 | ⬜ |
| 트레이 아이콘 DPI | 150% 모니터에서 아이콘이 흐리지 않음(24px 로 다시 그림) | ⬜ |
| 실시간 반영 | 설정 창에서 슬라이더를 끌면 창 밖 배지가 바로 바뀜. 취소 → 원래대로. 확인 → 유지·저장 | ⬜ |
| 설정 창 반응성 | 설정 창이 활성일 때 배지가 숨겨지고 깜빡이지 않음. 버튼·슬라이더가 즉시 반응. 메모장으로 전환하면 배지가 새 설정으로 보임 | ⬜ |
| 다크 모드 | Windows 앱 모드 어둡게: 설정·정보 창 배경·제목 표시줄·트레이 메뉴가 어둡고 글자가 읽힘 | ⬜ |
| 밝게/어둡게 전환 | 앱 실행 중 전환하면 트레이 메뉴 색이 따라옴(설정 창은 다시 열면 반영) | ⬜ |
| 고대비 모드 | 색을 입히지 않고 시스템 색 그대로. 카드 테두리가 보임 | ⬜ |
| 자동 시작 | 메뉴 체크 → 재로그인 시 실행. 설치 프로그램 옵션과 동일 값 | ⬜ |
| 업데이트 확인 | 수동: 최신/새 버전/오프라인 3가지 대화상자(TaskDialog). 새 버전이면 열기/나중에/건너뛰기 | ⬜ |
| 건너뛴 버전 | "이 버전 건너뛰기" 뒤 자동 확인에서 풍선이 안 뜸. 수동 확인에서는 다시 보임 | ⬜ |
| 정보 | 큰 아이콘·버전 표시, 링크(새 버전 확인·저장소·릴리스·설정 폴더·로그 폴더), 진단 정보 복사 → 붙여넣기 확인 | ⬜ |
| 단축키 변경 | 입력칸 클릭 후 Ctrl+Shift+F9 → 표시·즉시 동작·재시작 후 유지. Shift+H 만 누르면 경고음. 겹치는 조합이면 확인 시 경고 | ⬜ |
| 제외 앱 목록 | 실행 중 앱 목록에서 고르기, 직접 입력 + Enter, Delete 키로 삭제, 중복 추가 안 됨 | ⬜ |
| 니모닉·툴팁 | Alt+M 으로 배지 모양으로 이동, 항목에 마우스를 올리면 툴팁 | ⬜ |
| 직접 그린 컨트롤 | 토글 클릭·Space 로 전환(손잡이 이동), 슬라이더 드래그·화살표·휠, 타일 클릭·좌우 화살표, 색 견본 클릭·마지막 견본은 색 대화상자, Tab 이동 시 포커스 테두리 | ⬜ |
| DPI 150% 설정 창 | 토글·슬라이더·타일·견본이 겹치거나 잘리지 않고 배율에 맞음 | ⬜ |
| 제목 표시줄 색 | Windows 11: 제목 표시줄이 창 바탕과 같은 색. Windows 10: 밝게/어둡게만 반영 | ⬜ |
| 메뉴 아이콘 | 트레이 메뉴 항목 앞에 글리프 아이콘, 일시 중지 중이면 아이콘 뒤 강조색 상자, 상태 줄 앞에 작은 배지 그림. 언어를 바꿔 메뉴가 다시 만들어져도 아이콘 유지 | ⬜ |
| 언어 전환 | 설정 → 언어 → English: 설정 창이 같은 위치에 영어로 다시 열리고 바꾸던 값(슬라이더 등)이 유지됨. 트레이 메뉴·툴팁·정보 창이 영어. 취소하면 한국어로 복귀. 영어 Windows 에서 첫 실행 시 자동으로 영어 | ⬜ |
| 작은 아이콘 | 100% 트레이(16px)에서 커서·배지 도형이 뭉개지지 않음 | ⬜ |
| 중복 실행 | 두 번째 실행 → 기존 설정 창이 열림 | ⬜ |
| 첫 실행 | 풍선 알림 1회 | ⬜ |
| 설정 이관 | 0.x 의 exe 옆 파일 → %APPDATA% 로 복사 | ⬜ |

## 3. 환경

| 항목 | 확인 내용 | 결과 |
|---|---|---|
| DPI 100% / 150% / 200% | 배지 크기·위치 | ⬜ |
| 모니터 2대, 배율 다름 | 모니터 넘어갈 때 배율 변경 | ⬜ |
| 모니터 연결/해제 중 | 예외 없이 계속 동작(DisplaySettingsChanged) | ⬜ |
| 화면 잠금 → 해제 | 폴링 멈춤 → 재개 | ⬜ |
| 절전 → 복귀 | 배지 정상 | ⬜ |
| Program Files 설치 | 설정 저장이 됨(%APPDATA%) | ⬜ |
| 트리밍 빌드 | ColorDialog, HttpClient, Registry 동작 | ⬜ |
| ARM64 PC (Snapdragon 등) | `ImeBadge-Setup-<버전>-arm64.exe` 설치·실행. 작업 관리자 세부 정보의 "아키텍처" 열이 ARM64. 메모장·Chrome 에서 배지·한/영 판정 | ⬜ |
| ARM64 PC 에 x64 판 | x64 설치 프로그램도 설치·실행됨(에뮬레이션). 그 위에 arm64 판을 설치하면 앱 목록에 한 항목만 남고 설정 유지 | ⬜ |
| 유휴 CPU | 작업 관리자에서 1% 미만 | ⬜ |
| caret 이벤트 | 메모장에서 빠르게 타이핑·화살표 이동 시 배지가 지연 없이 따라옴. 마우스를 흔들어도 CPU 가 튀지 않음(포인터 이벤트는 걸러짐). `--debug` 로그에 5개 훅 registered | ⬜ |
| 장시간(8시간) 실행 | 메모리 증가 없음 | ⬜ |

## 4. 설치·제거

| 항목 | 확인 내용 | 결과 |
|---|---|---|
| 설치 중 실행 중이면 | 자동 종료 후 설치 | ⬜ |
| 자동 시작 옵션 | HKCU\...\Run 값 생성 | ⬜ |
| 제거 | 프로그램 종료, Run 값 삭제, 설정 파일은 유지 | ⬜ |
| 덮어쓰기 설치(업데이트) | 설정 유지 | ⬜ |
| SmartScreen | 서명 여부에 따른 경고 확인 | ⬜ |
